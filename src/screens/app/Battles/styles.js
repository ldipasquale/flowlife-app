import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  arenaContainer: {
    flexDirection: 'row',
  },
  arenaCapacityContainer: {
    flexGrow: 1,
    alignItems: 'flex-end',
    justifyContent: 'flex-end',
  },
})
