import { StyleSheet } from 'react-native'
import { fontSizes, others } from '@stylesheets'

export default StyleSheet.create({
  title: {
    letterSpacing: others.LETTER_SPACING,
    fontWeight: '600',
    fontSize: fontSizes.XXSMALL,
  },
})
